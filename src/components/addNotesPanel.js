import React from 'react';
import axios from 'axios';

const addNotesPanel = () => {
    return(
        <div className="notes-panel">
            <form>
                <h2>Add Note</h2>

            </form>
        </div>
    )
}

export default addNotesPanel;